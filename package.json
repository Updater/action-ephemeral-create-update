{
  "name": "action-ephemeral-create-update",
  "version": "1.0.3-0",
  "main": "dist/index.js",
  "repository": "git@github.com:Updater/action-ephemeral-create-update.git",
  "author": "apollorion <joey@apollorion.com>",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0"
  },
  "scripts": {
    "prebuild": "node -p \"'export const ACTION_VERSION = ' + JSON.stringify(require('./package.json').version) + ';'\" > src/version.ts",
    "build": "ncc build ./src/index.ts -o dist"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.33.3",
    "typescript": "^4.5.5"
  }
}
